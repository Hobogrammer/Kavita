import {TestBed} from '@angular/core/testing';
import {CacheService} from 'src/app/shared/_services/cacheService'
describe('CacheService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [CacheService]
    });
  });

  it('should be created', () => {

  });
});
